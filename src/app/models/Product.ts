export interface Product {
  title: string;
  subtitle: string;
  description: string;
  imageUrl: string;
  imageAltText: string;

  // price: string,
  // pricePer: string,
  // isAvailable: boolean,
  // createdBy: string
}

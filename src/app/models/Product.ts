export interface Product {
  id: string,
  title: string;
  subtitle: string;
  description: string;
  imageUrl: string;
  imageAltText: string;

  // price: string,
  // pricePer: string,
  // isAvailable: boolean,
  // createdBy: string
}

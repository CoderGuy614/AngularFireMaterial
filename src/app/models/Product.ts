export interface Product {
  id: string,
  title: string;
  subtitle: string;
  description: string;
  imageUrl: string;
  imageAltText: string;
  isAvailable: boolean;
  isBooked: boolean; 

  // price: string,
  // pricePer: string,
  // isAvailable: boolean,
  // createdBy: string
}
